spring:
  profiles:
    active: prod
  #mysql数据库配置
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://192.168.0.106:3306/wb_user
    username: root
    password: root
  #spring-data-jpa配置
  jpa:
    hibernate:
      ddl-auto: update
      use-new-id-generator-mappings: false
    show-sql: true

